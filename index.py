from Classes.Builder import Builder

Corporate_Database_Builder = Builder(str(input("Enter a date to retrieve the corporate metadata for the quarter of that date")))
"""
The builder which will build the database.
"""